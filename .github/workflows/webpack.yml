name: Build bundle

on:
    push:
        branches: ['main']
    pull_request:
        branches: ['main']

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v3

            - name: Use Node.js
              uses: actions/setup-node@v1
              with:
                  node-version: 18

            - name: Install & Build
              run: |
                  npm install
                  npx webpack
                  npm run build

            - name: Update bundle.js
              run: |
                  git add ./dist/
                  git commit -m "Create bundle" --no-verify
              # Don't trigger workflow

            # - name: Log in to npm
            #   run: npm login --registry=https://registry.npmjs.org/ --scope=editorjs-indent-tune --always-auth=false
            #   env:
            #       NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

            # - name: Publish to NPM
            #   run: npm publish --tag beta
            #   env:
            #       NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
